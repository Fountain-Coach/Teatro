TeatroCLI

Scope
- Command-line tools that leverage TeatroCore/Audio/GUI for scripting and pipelines.

Status
- CLI executables remain in the root package (`RenderCLI`, `teatro-play`, `TeatroPreviewDemo`).
- A separate package may be introduced later to enforce one-way dependencies.

